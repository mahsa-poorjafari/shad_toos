.form-group.upload-pic
  = picture.file_field :image, :class => "hide"
  = image_tag (picture.object.image.present? ? picture.object.image(:medium) : "missing.png"), :class => "enable_file_select", :style => "height:50px; width:50px" 

  .form-group
    = picture.label "توضیحات عکس", :class => "col-sm-3 control-label"   
    = picture.text_area :description, :rows => 2, :class => "form-control col-sm-3"
  
    = picture.link_to_remove image_tag ("Delete_Icon.png"), :class => "remove-task "
.clr-float
