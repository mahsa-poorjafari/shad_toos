!!!
%html
  %head
    %title 
      = t("rainbow") + ', ' + Category.all.collect(&:title).join(",") #{@head.fetch(:title, '') if @head}
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = favicon_link_tag 'favicon.png', rel:'shortcut icon', type: 'image/png'
    = csrf_meta_tags
    %meta{:charset => "utf-8"}
    %meta{:name => "keywords", :content => Category.all.collect(&:title_fa).join(" ") } 
    %meta{:name => "description", :content => "Rainbow, شرکت رنگین کمان, پارکت ، کاغذ دیواری، کفپوش"}
    %meta{content: "no", "http-equiv" => "imagetoolbar"}
    = yield(:refresh_meta) if content_for?(:refresh_meta)
    -#
      :javascript
        <!--
        //Disable right click script
        //visit http://www.rainbow.arch.scriptmania.com/scripts/
        var message="Sorry, right-click has been disabled";
        ///////////////////////////////////
        function clickIE() {if (document.all) {(message);return false;}}
        function clickNS(e) {if
        (document.layers||(document.getElementById&&!document.all)) {
        if (e.which==2||e.which==3) {(message);return false;}}}
        if (document.layers)
        {document.captureEvents(Event.MOUSEDOWN);document.onmousedown=clickNS;}
        else{document.onmouseup=clickNS;document.oncontextmenu=clickIE;}
        document.oncontextmenu=new Function("return false")
        // -->
          
  %body     
    .overlay
      %header
        #header-top        
        #logo-menu
          #logo
            = image_tag "logo.png"          
          .title-site
            %h3= t('rainbow')
          #mainmenu
            %nav
              -if I18n.locale == :fa        
                .menu_fa{:class => I18n.locale}
                  = render "shared/menu"
              - else
                .menu_en{:class => I18n.locale}
                  = render "shared/menu"
    #content{:class => I18n.locale}        
      = yield
    %footer{:class => I18n.locale}
      = render "shared/footer"
